jdk:
  - openjdk17

android:
  components:
    - build-tools-34.0.0
    - android-34
    - platform-tools

before_install:
  - chmod +x ./gradlew

install:
  - ./gradlew :analytics-sdk:assembleRelease --stacktrace --no-daemon
