jdk:
  - openjdk17

before_install:
# Remova essa linha se você não usa mais scripts
# - ./scripts/prepareJitpackEnvironment.sh

install:
  - echo "Using JDK 17"

script:
  - ./gradlew clean assemble publishToMavenLocal -PVERSION_NAME=${VERSION}
